module Session {
    class AthleteProfile {
        var ftp;
        var weight;

        function initialize(ftpValue, weightValue) {
            ftp = ftpValue;
            weight = weightValue;
        }
    }
}